# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code local config (project-scoped settings & dev launcher) — but
# skills are part of the project (e.g. /saveari) and travel with the repo.
.claude/*
!.claude/skills/

# vscode workspace
.vscode/
.vercel

# local working art / scratch assets (raw PSD slices etc. — not shipped)
00. * Assets/
public/room/mos.local-backup/

.vercel
.env*

# ctrl-s. (Ari's room) — "00. * Assets/" above already keeps the whole folder
# local, but style.md (the style law) and README.md (the folder convention) are
# committed, so re-include the folder and ignore the private part: memories/,
# which holds every family photo and every raw Gemini frame.
!00. Ctrl-S Assets/
00. Ctrl-S Assets/memories/
